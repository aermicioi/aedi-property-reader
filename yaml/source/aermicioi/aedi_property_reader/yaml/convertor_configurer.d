/**
License:
	Boost Software License - Version 1.0 - August 17th, 2003

	Permission is hereby granted, free of charge, to any person or organization
	obtaining a copy of the software and accompanying documentation covered by
	this license (the "Software") to use, reproduce, display, distribute,
	execute, and transmit the Software, and to prepare derivative works of the
	Software, and to permit third-parties to whom the Software is furnished to
	do so, all subject to the following:

	The copyright notices in the Software and this entire statement, including
	the above license grant, this restriction and the following disclaimer,
	must be included in all copies of the Software, in whole or in part, and
	all derivative works of the Software, unless such copies or derivative
	works are solely in the form of machine-executable object code generated by
	a source language processor.

	THE SOFTWARE IS PROVIDED "AS IS", WITHOUT WARRANTY OF ANY KIND, EXPRESS OR
	IMPLIED, INCLUDING BUT NOT LIMITED TO THE WARRANTIES OF MERCHANTABILITY,
	FITNESS FOR A PARTICULAR PURPOSE, TITLE AND NON-INFRINGEMENT. IN NO EVENT
	SHALL THE COPYRIGHT HOLDERS OR ANYONE DISTRIBUTING THE SOFTWARE BE LIABLE
	FOR ANY DAMAGES OR OTHER LIABILITY, WHETHER IN CONTRACT, TORT OR OTHERWISE,
	ARISING FROM, OUT OF OR IN CONNECTION WITH THE SOFTWARE OR THE USE OR OTHER
	DEALINGS IN THE SOFTWARE.

Authors:
	aermicioi
**/
module aermicioi.aedi_property_reader.yaml.convertor_configurer;

import aermicioi.aedi.storage.storage;
import aermicioi.aedi.storage.locator;
import aermicioi.aedi_property_reader.yaml;
import dyaml;
import std.experimental.logger;

/**
Create a convertor container with data source being xml document.

Params:
    locator = source of data for container to use to construct components.
Returns:
    yamlConvertorContainer
**/
auto yaml() {
    return yaml(new YamlLocator(new YamlNodePropertyAccessor()));
}

/**
ditto
**/
auto yaml(Locator!(Node, string) locator) {
    auto container = new YamlConvertorContainer;
    container.locator = locator;

    return container;
}

/**
Create a convertor container with data source being xml document.

Params:
    element = root element used as data source
Returns:
    yamlConvertorContainer
**/
auto yaml(Node element) {
    auto locator = new YamlLocator(new YamlNodePropertyAccessor());
    locator.yaml = element;
    return yaml(locator);
}

/**
Create a convertor container with data source being xml document.

Params:
    pathOrData = path to a xml file or xml data itself
    returnEmpty = wheter to return or not a locator with empty data source
Returns:
    yamlConvertorContainer
**/
auto yaml(string pathOrData, bool returnEmpty = true) {
    import std.file;

    try {

        if (pathOrData.exists) {
            return yaml(Loader(pathOrData).load());
        } else {
            return yaml(Loader.fromString(pathOrData.dup).load());
        }
    } catch (YAMLException e) {
        debug(trace) trace("Error parsing yaml: ", e);

        if (returnEmpty) {
            debug(trace) trace("Providing empty container");
            return yaml();
        }

        throw new Exception("Could not create yaml convertor container from file or content passed in pathOrData: " ~ pathOrData, e);
    }
}

auto factoryDefaultConvertor() {

    import aermicioi.aedi_property_reader.core.convertor_factory : ConvertorFactory;
    import aermicioi.aedi_property_reader.yaml.yaml_factory : YamlConvertorFactory;
    import aermicioi.aedi_property_reader.core.convertor_factory_impl : TypeGuessingConvertorFactory, ObjectWrappingConvertorFactory;
    import aermicioi.aedi_property_reader.yaml.type_guesser : YamlTypeGuesser;
    import aermicioi.aedi.storage.object_storage : ObjectStorage;
    import std.meta : AliasSeq;
    import std.datetime;
    import dyaml;

    TypeGuessingConvertorFactory!Node factory = new TypeGuessingConvertorFactory!Node;
    auto convertors = new ObjectStorage!(ConvertorFactory!(Node, Object), TypeInfo)();

    factory.guesser = new YamlTypeGuesser();
    factory.convertors = convertors;

    foreach (Type; AliasSeq!(
        bool,
        long, real, ubyte[],
        SysTime, string, Node.Pair[],
        Node[]
    )) {
        convertors.set(
            new ObjectWrappingConvertorFactory!(YamlConvertorFactory!Type)(new YamlConvertorFactory!Type),
            typeid(Type)
        );
    }

    return factory;
}

private auto yamlAccessor() {
    import aermicioi.aedi_property_reader.core.accessor;

    return new PropertyPathAccessor!Node(
        new YamlNodePropertyAccessor(),
        new YamlIntegerIndexAccessor()
    );
}